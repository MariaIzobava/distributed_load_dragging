# Overview

This folder contains code for autotuning factor graph params.

## Usage

**One-time command** for creating `build/` repo:

```shell
$ cmake -S . -B build
```

After that to rebuild and rerun the code run:

```shell
$ cmake --build build
$ ./build/params_tuning
```